/**
 * Компонент MeshSelector
 * 
 * Используется для выбора конкретной сетки (mesh) в выбранном узле 3D-модели.
 * Например, если узел - это "сиденье", то здесь можно выбрать
 * между разными вариантами геометрии сидений.
 * 
 * Этот компонент отображает список доступных сеток для выбранного узла
 * и позволяет пользователю переключаться между ними.
 */
import MeshSelector from "./MeshSelector"

export default MeshSelector
