/**
 * Компонент ProductSelector
 * 
 * Позволяет выбрать 3D-модель продукта для конфигурирования.
 * В данном случае сейчас используется только одна модель - "My Custom Model".
 * 
 * При выборе продукта загружается соответствующая 3D-модель
 * со всеми настройками узлов, материалов и мешей.
 */
import ProductSelector from "./ProductSelector"

export default ProductSelector
